# SQRA environment configuration (copy to .env for local development).
# On Hugging Face Spaces these are injected as repository secrets.

# Persistent storage root. Defaults to /data (the HF Pro mounted volume).
SQRA_DATA_DIR=/data

# Market data provider credentials. Injected on HF as HF_SECRET_*.
HF_SECRET_MARKET_API_KEY=
